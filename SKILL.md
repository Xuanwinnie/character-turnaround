---
name: character-turnaround
description: 從一張正面角色參考圖分析角色身份、建立可重複使用的 Character Bible，並在設定確認後生成正面、四分之三正面、側面、背面四張獨立角色參考圖。適用於角色、吉祥物、品牌 IP、遊戲、動畫與繪本角色的角色基準包；不適用於一般場景插畫或直接大量生成素材。
---

# Character Turnaround

把一張正面角色圖整理成可供後續 IP 素材生成使用的角色基準包。核心成果是：可編輯的 Character Bible、四張獨立視角圖，以及可追溯的 QA 紀錄。核心流程是媒材中立的：可用於 2D、3D 或兩者共用的角色參考；3D 資訊僅在需要交接建模或綁定時才加入。本 Skill 與 `ip-asset-generator` 透過相容欄位協作，不修改或覆蓋另一個 Skill。

## 入口與模式

先確認使用者提供可讀的正面角色圖。若沒有，要求一張正面參考圖，不能假裝已完成分析或生圖。若有多張圖，指定哪一張是主正面參考；其他圖只作補充來源。

依需求選擇：

- **快速角色基準圖**：角色設定已明確，仍要先整理身份鎖定並讓使用者確認，再生成四張圖。
- **純圖片模式**：只需要四張角色基準圖，不建立或交付 Character Bible；仍需在內部擷取身份錨點、確認未知背面細節並執行 QA。
- **角色設定規劃**：先輸出已確認、合理推測、待確認三類內容，等待確認後才建立正式 Bible 與生圖。
- **完整角色基準包**：除 Bible 與四張核心角色基準圖外，再提供表情、動作、服裝、配色與後續素材建議；MVP 不要自動生成這些延伸圖片。

使用者未指定模式時，採「角色設定規劃」。即使使用者要求直接快速出圖，也不可跳過確認關鍵未知細節。

純圖片模式的交付只包含四張圖片與簡短 QA 結論，不輸出 `character-bible.yaml` 或 `turnaround-qa.yaml`。若使用者之後要交給 `ip-asset-generator` 長期使用，應重新執行角色設定規劃，建立正式 Bible 與可保存的 QA 紀錄。

## 媒材模式與下游交接

在角色設定規劃中，確認 `asset_mode: 2d|3d|both`。未指定時預設為 `2d`，不可要求 2D 使用者補填建模、拓撲或骨架資訊。

- **2d**：以畫風、線條、色彩、平面遮擋與後續插畫、貼圖、漫畫或場景素材為重點。
- **3d**：在共用身份與空間規則之外，確認可觀察的厚度、立體結構、材質、可動部位與建模限制；未知內容仍必須列入 `needs_confirmation` 或標示為推測。
- **both**：維持一份共用 Bible，2D 與 3D 欄位各自可選填；不可讓 3D 的推測覆寫已確認的 2D 身份錨點或視覺語言。

`downstream_handoff.two_d` 與 `downstream_handoff.three_d` 是可選欄位，描述後續用途而非啟動生成、建模、LoRA 訓練或綁定工作。本 Skill 不內建 3D mesh、rig、拓撲或動畫製作流程。

## 分析規則

只把參考圖或使用者明確提供的資料寫成已確認事實。回應必須區分：

- **已確認**：可由圖像直接觀察或使用者明確說明的內容。
- **合理推測**：可作創作建議，但尚未成為固定設定。
- **待確認**：正面圖無法判斷的內容，尤其是後腦、衣服背面、配件背側、尾巴／翅膀背面、鞋子後側與隱藏結構。

至少擷取 3 個身份錨點，最好 4–6 個，最多 8 個；為每個錨點標示優先級。錨點應是跨視角仍可辨識的特徵，例如輪廓、臉型、眼睛、主色、永久標記或招牌配件。不得把推測的背面細節寫入 `fixed_rules`。

同時由 AI 根據圖檔建立 `proportion_lock` 的相對比例估算，包含頭身比、身體寬度、四肢長度、角色在畫布中的高度、腳底基準線與配件相對位置。這些是穩定生成的輔助約束，不是像素級測量；每項要標示 `source: ai-estimated-from-reference` 與可信度。純圖片模式只在內部使用，角色設定模式才交付並讓使用者確認。

若角色有會因視角改變而容易畫錯的結構或特徵，必須建立 `spatial_feature_contract`。它不是泛泛的描述，而是每個關鍵項目的可驗證契約：它位於角色的哪個表面、是否有角色自身左右、與身體或其他物件的連接方式、朝向、前後遮擋、重複數量、圖樣／材質連續性，以及每個視角的預期狀態。項目可包含五官、四肢、尾巴、翅膀、耳朵、衣物、配件、髮型、色塊、圖樣或任何會影響輪廓與辨識的部位；只納入此角色實際存在且屬於高風險的項目。正面圖無法判斷的值必須標為 `needs_confirmation`；不得自行把它寫成固定事實。

## 跨視角空間合理性

生成轉面圖時，不能只複製正面圖案或將正面左右鏡像；必須先把角色視為同一個具備前、後、左、右空間關係的立體角色，再決定每個特徵在新視角中的可見性與位置。

- 先判斷每個配件的附著位置、朝向與遮擋關係：可見性必須由視角、附著面與角色身體的遮擋幾何推導，不使用「正面出現／背面消失」的二分法。例如胸前的蝴蝶結、領巾結、徽章或口袋，在四分之三正面與側面可能部分可見，但若位於前側且被身體遮住，背面不得顯示；只有確實繞過頸部或延伸到背面的布料／結構才可見。這些例子不是固定清單，任何配件都適用同一原則。
- 對尾巴、耳朵、斑紋、背帶等非對稱特徵，建立「角色自身左右」而非「畫面左右」的標記，並依其實際附著點、朝向、旋轉角度與遮擋關係推導新視角位置；不可用固定的畫面左右規則任意鏡像或換邊。
- 對有根部或分段的延伸肢體（尾巴、翅膀、角、辮子等），明確鎖定根部所在身體區域、根部應被身體遮住的程度，以及「由根部到末端」的色段／花紋順序。除根部外，還必須記錄末端相對於角色自身中線的側別、可觀察的彎曲路徑，以及它與固定地標（耳朵、肢體、色塊或配件等）的相對位置。這些都是同一個物件的空間狀態，不是可由每張新圖重新設計的姿勢。禁止只描述畫面中的顏色位置，因為角色旋轉後畫面左右會改變。若根部位置、末端側別或曲線走向無法由參考圖確認，必須在生成前請使用者選定，或採保守補完並標記為推測。
- 對延伸肢體的跨視角換算，先以「角色自身座標」保存根部、每一個可辨識轉折點與末端，再依指定鏡頭／旋轉方向投影到畫面。不可用新視角中看起來較平衡、較可愛或較自然的輪廓取代已確認的側別或曲線。若參考圖可確認末端與固定地標位於同一側或相對側，生成後該關係必須在旋轉投影後保持成立。
- **一般四足動物的尾巴預設**：若正面參考中只看見尾巴露出、但看不見根部，不能把露出位置當成側邊附著點。除非參考圖或使用者明示例外，應保守推定根部位於臀部中央背面／尾椎基部。背面必須看見尾巴直接從該區域長出；尾巴可在離開根部後彎向一側，但不得從身體側邊、身後、空隙或被完整身體輪廓遮住的位置開始。
- 對一般四足動物的背面，尾巴根部是可驗證的前後圖層關係：根部須在後軀表面可見，身體外框不得完整地從根部前方穿過。若造型刻意將根部遮住，必須有參考圖或使用者明確確認；否則視為不合理的附著。
- 對臉部特徵，視角名稱必須轉換成可驗證的數量規則。三分之四正面預設為近側眼完整可見、遠側眼至少部分可見；若角色設定例外（獨眼、遮眼瀏海、面具等）才可不同。側面預設只可見一眼；背面不得出現前側五官。不可接受把三分之四正面退化為側面，或以「風格化」為由省略應可見的遠側眼。
- 對所有視角，先驗證姿態與鏡頭是否真的符合指定角度，再檢查局部細節。三分之四正面必須同時呈現前面與其中一個側面的資訊；側面只能呈現單側輪廓；背面不得混入只有前面才可見的資訊。不得以含糊的斜視、鏡像、漂浮結構或不一致的透視冒充轉面。若無法由參考圖合理推出某個隱藏面的形狀，應停在 `needs_confirmation` 或採保守補完並標記，不能為了填滿畫面而發明結構。
- 背面未知細節可以保守補完，但不得新增與正面結構矛盾的配件、正面裝飾或不合理的附著點。推測的是材質與遮擋後的形狀，不是重新發明配件位置。
- 生成前在內部完成一份「視角轉換檢查表」：對每個配件與非對稱特徵記錄附著部位、角色自身左右、朝向、四個視角的預期可見程度（不可見／部分可見／完整可見）、遮擋關係與根部連接。對 `spatial_feature_contract` 內標為 `critical` 的項目，還必須記錄每視角的預期數量、色段順序、根部／轉折點／末端的預期畫面位置，以及 `fail` 條件。這份檢查不必在純圖片模式交付，但必須影響 prompt 與 QA。

## 確認閘門

正式生成前，呈現簡潔確認卡，至少包含：角色身份、媒材模式、身份錨點、固定外觀、服裝／配件、色彩與畫風、待確認的側面／背面細節，以及是否允許依整體風格合理補完。當模式為 `3d` 或 `both` 時，再列出已確認與待確認的立體結構、材質與可動部位；使用者未確認前，只能提供規劃與 prompt 草稿，不得生成正式基準圖。

若未知細節會影響角色識別，提供兩個選項：使用者補充側面／背面參考，或明確允許保守補完；後者要在 Bible 與 QA 標記為推測／AI 補完。

## Character Bible

建立 `character-bible.yaml`，欄位與 IP Asset Generator 相容。完整欄位說明見 [character-bible-schema.md](references/character-bible-schema.md)。至少包含：

```yaml
character_id: <kebab-case-id>
asset_mode: 2d|3d|both
reference_confidence: high|medium|low
reference_sources: []
identity_anchors: []
fixed_rules: []
needs_confirmation: []
allowed_variations: []
forbidden_changes: []
personality: {}
story_role: {}
world_setting: {}
visual_language: {}
palette: []
materials: []
expression_vocabulary: []
costume: {}
signature_accessories: []
spatial_feature_contract: []
proportion_lock: {}
downstream_handoff:
  two_d: {}
  three_d: {}
output_requirements:
  width: <pixels>
  height: <pixels>
  transparent_background: false
  preserve_original_front: true
turnaround_requirements:
  views: [front, three_quarter_front, side, back]
  separate_images: true
  neutral_pose: true
```

未知欄位使用 `needs_confirmation` 或明確的 `status`，不要用看似確定的空泛敘述填滿。`allowed_variations` 只列可變項目；物種／角色定位、臉型、身體比例、主色、永久標記、招牌配件與視覺語言原則上不得變更。當 `asset_mode: 2d` 時，`downstream_handoff.three_d` 可省略或設為 `enabled: false`。

## 角色基準圖生成

核心輸出是四張獨立圖片：正面、四分之三正面、側面、背面。先保留原始正面圖。若使用者要求保留它，`character-front.png` 可直接引用原始檔，不必重新生成；若要求統一畫布或重新繪製，才生成新的正面圖。其餘三個視角必須各自獨立生成，每張只含一個角色。

四張圖共同鎖定：同一角色、同一比例、同一鏡頭距離、同一站立基準線、中性站姿、同一畫風、同一光線與背景處理。套用 `proportion_lock` 的相對估算，但若 QA 顯示估算與參考圖衝突，以可觀察的參考圖與使用者確認為準。禁止合併成角色轉面拼版。每張不得加入動作、道具、文字、Logo 或額外角色。若需透明背景，四張都要一致。

對有根部的高風險結構，生成順序不可獨立猜測：先由正面建立空間契約，再生成並 QA 側面；側面通過後，背面必須沿用同一根部、轉折點與末端的角色自身位置、朝向、曲線走向、色段順序與遮擋關係。側面圖是背面生成的空間驗證依據，不得把未經 QA 的側面輸出當成新事實；若側面無法確認這些關係，背面必須停在 `needs_confirmation` 或採已標記的保守補完。

生成 prompt 順序：身份鎖定與參考圖用途 → 必須保留的 3–8 個錨點與 fixed rules → 先寫出該視角的方位與鏡頭條件 → `spatial_feature_contract` 中該視角的數量、連接方式、角色自身左右、根部／轉折點／末端的位置、曲線走向、圖樣連續性與遮擋規則 → 中性站姿、畫布、比例、基準線與鏡頭 → visual language、配色、材質、光線 → hard avoids 與未知細節政策。不可只寫「保留某個特徵」；必須把 `critical` 特徵寫成可判定的條件，例如「三分之四正面必須有兩眼，遠側眼部分可見」或「延伸構件從已確認的後側附著點長出，根部被身體遮擋，所有可辨識轉折點與末端均依角色自身座標換算，圖樣順序不可反轉」。若延伸構件在參考圖中可確認末端與某固定地標的相對側別，prompt 必須列出該關係在目前視角投影後的預期畫面位置，並禁止末端換到相反側。對一般四足動物的背面尾巴，需明示「尾巴從臀部中央背面／尾椎基部可見地長出；根部在後軀表面可見，身體外框不可穿過根部；離開根部後才可沿已確認的角色自身曲線彎向指定側」。對背面視圖也要明確寫出「位於前側且被身體遮擋的正面專屬元素不可見」，並說明哪些結構因繞過身體或延伸到背面而可見；不能只寫「正面配件不可見」或「保留配件」。

如果圖像生成工具可用，逐張生成並回傳圖片；不可將未生成的結果說成已完成。若工具不可用，輸出四份可複製 prompt，明確說明尚未產生圖片。

固定命名：

```text
<character-id>-front.png
<character-id>-three-quarter-front.png
<character-id>-side.png
<character-id>-back.png
<character-id>-bible.yaml
turnaround-qa.yaml
```

有角色 ID 時使用 `<character-id>-bible.yaml`；沒有角色 ID 時使用 `character-bible.yaml`。圖片檔案則使用 `character-front.png`、`character-three-quarter-front.png`、`character-side.png`、`character-back.png`。

## QA 與重試

逐張檢查並記錄：身份、臉型／頭部比例、身體比例、比例鎖定表的相對關係、服裝、招牌配件、主色／輔色、指定視角是否真的成立、角色自身左右是否正確、配件附著點是否連續、各配件在該視角的預期可見程度是否符合、前後遮擋是否合理、非對稱特徵是否依空間關係連續、側背未知細節、角色大小／基準線、單一角色、無文字／Logo／多餘道具，以及原始畫風。對 `spatial_feature_contract` 的每個 `critical` 項目，逐視角驗證數量、連接方式、遮擋、根部／轉折點／末端的角色自身側別與投影後畫面位置、方向、曲線與圖樣／材質連續性。若延伸結構的末端在參考圖中有可驗證的側別或相對地標關係，轉面後末端沒有依視角換算至預期位置，或曲線被改成相反方向，直接 `fail`；不得因新輪廓較自然或較平衡而放行。對一般四足動物的背面尾巴，以下任一情形直接 `fail`：根部不在臀部中央背面／尾椎基部（除非有明確例外）；根部不可見卻沒有已確認的遮擋理由；身體外框完整從根部前方穿過；只把尾巴畫成從側邊、身後或空隙伸出。對正面到背面的非對稱頭部、耳朵與身體花色，另檢查是否隨 180 度旋轉換到相反畫面側，而不是留在原本畫面側。`3d` 或 `both` 模式另檢查已確認的立體結構、材質與可動部位在各視角是否自洽；未確認內容只能標為推測或待確認。若任一視角不是指定角度、出現不可能的連接或穿模、前側元素出現在不應可見的面、輪廓或左右關係不連續、重複數量不合理、圖樣／材質無故跳變，或任一 `critical` 項目不符契約，該視角直接 `fail`，最多重試一次。

使用 [turnaround-qa-template.yaml](references/turnaround-qa-template.yaml) 記錄每個 view 的 `status: pass|fail|needs_review`、檢查項目、失敗原因、是否重試與限制。只有失敗的視角可以重試，每個視角最多一次；已通過的視角不要重生。重試後仍失敗，明確說明視角與失敗特徵，並建議補充參考圖或人工修訂，不宣稱像素級一致。

## 與 IP Asset Generator 的交接

交付時把 Bible 與四張視角圖視為一個 reference pack。使用共同欄位 `identity_anchors`、`fixed_rules`、`allowed_variations`、`visual_language`、`reference_confidence`；角色轉面圖的 QA 結果只增加約束，不改寫未確認設定。後續 IP Asset Generator 應以這些檔案作為角色參考，並仍遵守其身份錨點與變化預算。若有 3D 下游，僅交接 `downstream_handoff.three_d` 中已確認的資訊；它是建模參考，不保證自動產生 mesh 或 rig。

## 交付內容

一般模式最後交付生成成功的四張圖片或明確 prompt、Character Bible、QA 紀錄、每張視角的短摘要，以及仍待使用者決定的項目。純圖片模式只交付四張圖片或明確 prompt 與簡短 QA 結論，不建立或交付 Character Bible／QA YAML。另列出「建議補充參考圖」，例如頭部特寫、招牌配件特寫、手腳／尾巴／翅膀等特殊結構、主要表情與姿勢參考；這些不是核心輸出的必要項目。若使用者要長期經營 IP，再進入完整角色基準包，增加表情表、動作規劃、服裝規劃、配色規範與素材延伸建議。指出會限制一致性的因素，尤其是單張正面圖無法提供背面資訊、透明背景未能保證，或生成工具未支援參考圖控制。
